<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:layout_width="match_parent">

    <GridLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:context=".MainActivity"
        tools:viewBindingIgnore="true">

        <Button
            android:id="@+id/wail_btn"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_row="0"
            android:layout_column="0"
            android:layout_margin="16dp"
            android:text="@string/wail_btn" />

        <Button
            android:id="@+id/yelp_btn"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_row="0"
            android:layout_column="1"
            android:layout_margin="16dp"
            android:text="@string/yelp_btn" />

        <Button
            android:id="@+id/horn_btn"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_row="1"
            android:layout_column="0"
            android:layout_margin="16dp"
            android:text="@string/horn_btn" />

        <Button
            android:id="@+id/phsr_btn"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_row="1"
            android:layout_column="1"
            android:layout_margin="16dp"
            android:text="@string/phsr_btn" />

        <Button
            android:id="@+id/stop_btn"
            android:layout_width="96dp"
            android:layout_height="96dp"
            android:layout_gravity="fill_horizontal"
            android:layout_row="2"
            android:layout_column="0"
            android:layout_columnSpan="2"
            android:layout_margin="16dp"
            android:text="@string/stop_btn" />

    </GridLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
